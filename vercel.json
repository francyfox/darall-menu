{
  "version": 2,
  "installCommand": "pnpm i && npx prisma generate",
  "builds": [
    {
      "src": "./dist/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist"
    }
  ]
}