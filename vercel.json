{
  "version": 2,
  "buildCommand": "prisma generate && tsc",
  "builds": [
    {
      "src": "./dist/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist"
    }
  ]
}