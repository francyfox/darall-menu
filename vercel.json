{
  "version": 2,
  "installCommand": "pnpm i",
  "buildCommand": "npx prisma generate && pnpm run build",
  "builds": [
    {
      "src": "./dist/*.js",
      "use": "@vercel/node"
    }
  ],
  "routes": [
    {
      "src": "/(.*)",
      "dest": "/dist"
    }
  ]
}